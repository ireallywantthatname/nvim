return {
	"catppuccin/nvim",
	config = function()
		vim.cmd.colorscheme("catppuccin")
		local config = require("catppuccin")
		config.setup({
			flavour = "mocha",
			transparent_background = true,
			term_colors = true,
		})
	end,
}
